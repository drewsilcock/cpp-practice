line-count
==========

This is an incredibly simple program to count the number of lines in a file.

Compilation
-----------

Just run `make`.

Usage
-----

Run:

```bash
./line-count myfile
> Number of lines: 56
```

Simple as.
